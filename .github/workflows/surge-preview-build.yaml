name: Surge Preview Build

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
    name: Build Preview
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout PR code
      uses: actions/checkout@v5

    - name: Build docs (Pantheon/ccutil styling)
      run: bash build_docs_preview

    - name: Save PR number
      if: success()
      run: |
        mkdir -p ./preview-build
        echo ${{ github.event.pull_request.number }} > ./preview-build/pr_number.txt

    - name: Upload build artifact
      uses: actions/upload-artifact@v4
      if: success()
      with:
        name: preview-build
        path: |
          dist/
          preview-build/pr_number.txt
